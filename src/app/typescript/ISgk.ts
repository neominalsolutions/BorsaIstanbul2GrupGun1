// baseden gelmeyen yeni bir yeteneği class kazandırmak için interface kullandık
export interface ISgk {
  pay(): void; // interface içerisinde propert method tanımı yaparız ve içini boş bırakırız
  paid: boolean;
}
